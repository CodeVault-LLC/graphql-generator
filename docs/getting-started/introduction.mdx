# Introduction to CodeVault's GraphQL Code Generator
GraphQL Code Generator is a tool that generates code from your GraphQL schema and operations.

# A simplistic GraphQL Developer Experience
To illustrate the easy workflow of GraphQL Code Generator, let's consider a simple example. Imagine you have a GraphQL schema that looks like this:

```graphql
type Query {
  hello: String
}
```
